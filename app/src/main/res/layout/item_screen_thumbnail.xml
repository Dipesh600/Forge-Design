<?xml version="1.0" encoding="utf-8"?>
<!-- Single screen thumbnail card shown in the canvas grid -->
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="200dp"
    android:layout_height="match_parent"
    android:layout_margin="8dp"
    app:cardCornerRadius="16dp"
    app:cardBackgroundColor="#12121F"
    app:cardElevation="8dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <!-- Screen thumbnail image — full card width, phone aspect ratio -->
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:background="#1A1A2E">

            <!-- Screenshot shown via Glide -->
            <ImageView
                android:id="@+id/imgScreenshot"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="centerCrop"
                android:contentDescription="Screen preview" />

            <!-- Dark placeholder while image loads -->
            <View
                android:id="@+id/shimmerThumb"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="#1E1E2E" />

            <!-- Tap to view fullscreen overlay -->
            <View
                android:id="@+id/overlayTap"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@android:color/transparent"
                android:clickable="true"
                android:focusable="true" />

            <!-- Screen number badge -->
            <TextView
                android:id="@+id/tvScreenNumber"
                android:layout_width="28dp"
                android:layout_height="28dp"
                android:layout_gravity="top|end"
                android:layout_margin="8dp"
                android:gravity="center"
                android:textSize="11sp"
                android:textColor="#FFFFFF"
                android:textStyle="bold"
                android:background="@drawable/bg_screen_badge"
                tools:text="1" />

        </FrameLayout>

        <!-- Screen name at bottom -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingHorizontal="12dp"
            android:paddingVertical="10dp">

            <TextView
                android:id="@+id/tvScreenName"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="#FFFFFF"
                android:textSize="13sp"
                android:textStyle="bold"
                android:maxLines="1"
                android:ellipsize="end"
                tools:text="Home Dashboard" />

            <TextView
                android:id="@+id/tvScreenStatus"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="#7C4DFF"
                android:textSize="11sp"
                android:layout_marginTop="2dp"
                android:maxLines="1"
                tools:text="Generated" />

        </LinearLayout>

    </LinearLayout>

</androidx.cardview.widget.CardView>
